[user]
    name = Florian Spieß
    email = business@minnced.club
[alias]
    ls = log --pretty='tformat:"%Cred%h %Cgreen%s %Cblue-- %Creset%an %C(auto)<%ae>%d%Creset"'
    ll = log --pretty='tformat:"%Cred%h %Cgreen%s %Cblue %C(auto)%d%Creset%nAuthor: %an <%ae>%nDate: %cD%n%n%w(70,4,4)%-b"'
    mt = mergetool
    dt = difftool
[commit]
    gpgSign = true
[tag]
    gpgSign = true
[core]
    editor = vim
    pager = less -R
    autcrlf = input
    whitespace = trailing-space,space-before-tab
    excludefile = ~/.gitignore_global
[merge]
    tool = vimdiff
[diff]
    tool = vimdiff
[help]
    autocorrect = 2
